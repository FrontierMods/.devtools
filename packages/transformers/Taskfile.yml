version: "3"

tasks:
    build:
        desc: "Build Transformers"
        sources:
            - "src/**/*.ts"
            - "tsconfig.json"
            - "../../tsconfig.base.json"
        generates:
            - "dist/**/*.js"
            - "dist/**/*.d.ts"
        cmds:
            - bun run build

    typecheck:
        desc: "Typecheck Transformers"
        cmds:
            - bun run typecheck

    lint:
        desc: "Lint Transformers"
        cmds:
            - bun run lint

    format:
        desc: "Format Transformers"
        cmds:
            - bun run format

    check:
        desc: "Check formatting on Transformers"
        cmds:
            - bun run format:check

    ci:
        desc: "Run all CI checks"
        cmds:
            - task: typecheck
            - task: lint
            - task: check
