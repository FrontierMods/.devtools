# `init`

## Purpose

`init` lets mod authors using the toolkit to quickly scaffold a toolkit-compatible mod.

## Success Criteria

`init` does its job well if:

- running it in a fresh directory yields a structure the toolkit can immediately build and the game can recognize
- running it again changes nothing and says so
- it never overwrites or discards existing content
- the scaffolded mod requires no further setup
